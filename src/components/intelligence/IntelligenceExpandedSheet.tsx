import React from 'react';
import { View } from 'react-native';

/**
 * Placeholder for a full-screen bottom sheet detail view.
 * Currently the card expands in-place; this component is reserved
 * for a future dedicated intelligence detail screen.
 */
export default function IntelligenceExpandedSheet(): React.JSX.Element {
  return <View />;
}
